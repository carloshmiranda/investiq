# Neon PostgreSQL (installed via Vercel Marketplace — run: vercel env pull .env)
DATABASE_URL=postgresql://user:pass@ep-xxx.eu-west-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL_UNPOOLED=postgresql://user:pass@ep-xxx.eu-west-1.aws.neon.tech/neondb?sslmode=require

# JWT — generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Encryption — generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Anthropic
ANTHROPIC_API_KEY=

# App
VITE_API_URL=https://your-app.vercel.app
